export type ReaderRenderMode = 'layout' | 'text'

export type ReaderPageTool = 'text-selection' | 'rect-selection' | 'drawing'

export type ReaderColorOption = {
  readonly name: string
  readonly color: string
}
